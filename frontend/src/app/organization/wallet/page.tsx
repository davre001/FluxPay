'use client';

// Organization wallet page — reuses the same design as creator wallet
// Just imports with organization context label
import CreatorWalletPage from '@/app/creator/wallet/page';

export default function OrgWalletPage() {
  return <CreatorWalletPage />;
}
