'use client';

// Organization reputation page — reuses the same design as creator reputation
import CreatorReputationPage from '@/app/creator/reputation/page';

export default function OrgReputationPage() {
  return <CreatorReputationPage />;
}
